app:
  log_level: "info"
  host: "0.0.0.0"
  port: 8080

database:
  host: "postgres"  # 또는 PostgreSQL 컨테이너 이름
  port: 5432
  user: "testuser"
  password: "testpassword"
  database_name: "escape"
  schema_name: "orders"
  ssl_mode: "disable"
